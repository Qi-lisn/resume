# ATS 简历优化工具 - 项目文档

## 项目概述

AI 驱动的智能 ATS 简历优化工具，支持从简历文件或职位描述中提取信息，并进行多维度优化分析。

## 技术栈

- **框架**: Next.js 16.x (App Router)
- **语言**: TypeScript
- **UI**: React
- **样式**: Tailwind CSS
- **Markdown 渲染**: react-markdown
- **大模型**: OpenAI SDK（兼容 OpenAI API 格式）
- **文件解析**: mammoth, tesseract.js

## 环境变量配置

项目使用环境变量配置大模型 API，需创建 `.env.local` 文件（参考 `.env.example`）：

```env
# API 基础 URL（兼容 OpenAI API 格式）
OPENAI_API_BASE_URL=https://api.openai.com/v1

# API 密钥
OPENAI_API_KEY=your_api_key_here

# 使用的模型名称
OPENAI_MODEL=gpt-4o
```

### 支持的提供商

通过修改 `OPENAI_API_BASE_URL` 和 `OPENAI_API_KEY`，可兼容以下提供商：

- **OpenAI**: `https://api.openai.com/v1`
- **DeepSeek**: `https://api.deepseek.com/v1`
- **Moonshot**: `https://api.moonshot.cn/v1`
- **通义千问**: `https://dashscope.aliyuncs.com/compatible-mode/v1`

## 页面结构

### 主页布局 (左右分栏)

**左侧区域** (50% 宽度):
1. 原简历输入区：
   - 文件上传区域（支持点击上传或拖拽）
   - 多行文本输入框
2. 目标岗位 JD 输入区：
   - 文件上传区域（支持点击上传或拖拽）
   - 多行文本输入框（支持 Ctrl+V / Cmd+V 粘贴截图）
3. "开始优化" 按钮

**右侧区域** (50% 宽度):
1. 优化结果标签切换（全部、关键词匹配、简历重写、STAR 原则、ATS 评分）
2. 优化结果展示区域
3. 使用 Markdown 格式渲染输出

## 功能说明

### 文件上传与解析

#### 简历上传区
- **支持格式**: Word (.docx), PNG (.png), JPG (.jpg/.jpeg)
- **上传方式**: 点击选择文件 或 拖拽文件到上传区域
- **解析后**: 自动将提取的文本回填到下方的文本输入框
- **备注**: PDF 暂不支持

#### JD 上传区
- **支持格式**: PNG (.png), JPG (.jpg/.jpeg)
- **上传方式**:
  - 点击选择文件
  - 拖拽文件到上传区域
  - 直接在文本框中使用 `Ctrl+V` (Windows) 或 `Cmd+V` (Mac) 粘贴截图
- **OCR 识别**: 使用 tesseract.js 进行图片文字识别
- **解析后**: 自动将识别的文字回填到下方的文本输入框

#### 解析状态提示
- 解析过程中显示"正在解析文件..."的提示信息
- 解析成功后自动清除提示
- 解析失败显示错误信息，3秒后自动消失

### AI 优化功能

#### 1. 关键词匹配分析
- **功能**: 分析简历与 JD 的关键词匹配度
- **输出**:
  - JD 中的核心技能关键词列表
  - 简历中匹配的关键词
  - 缺失的关键词
  - 额外加分项
  - 匹配率统计

#### 2. 简历内容重写
- **功能**: 根据 JD 要求重写简历内容
- **输出**: 完整的优化后简历
- **原则**:
  - 保留核心信息和真实经历
  - 根据 JD 要求调整用词
  - 使用行业术语和 ATS 友好关键词
  - 优化表达方式

#### 3. STAR 原则优化
- **功能**: 将工作经历改写为 STAR 格式
- **STAR 原则**:
  - **S**ituation: 工作背景和情境
  - **T**ask: 面临的任务和挑战
  - **A**ction: 采取的具体行动和解决方案
  - **R**esult: 取得的成果和量化结果
- **输出**: 优化后的工作经历（STAR 格式）

#### 4. ATS 评分
- **功能**: 计算简历的 ATS 通过率评分
- **评分维度** (总分 100):
  - 关键词匹配度 (30分)
  - 格式规范性 (20分)
  - 内容完整性 (20分)
  - 表达清晰度 (15分)
  - 专业术语使用 (15分)
- **输出**:
  - 总评分 (0-100)
  - 评分等级 (优秀/良好/中等/需改进)
  - 各维度详细得分
  - 评分等级颜色标识

#### 5. 改进建议
- **功能**: 基于分析结果给出具体的改进建议
- **输出**: 3-5 条具体建议
- **建议类型**:
  - 技能补充建议
  - 内容格式优化建议
  - ATS 通过率提升建议

### 加载状态说明

| 状态 | 显示内容 | 说明 |
|------|----------|------|
| `idle` | "开始优化" | 初始状态 |
| `analyzing` | "分析中..." (齿轮动画) | 第一阶段：分析简历与 JD |
| `optimizing` | "优化中..." (齿轮动画) | 第二阶段：生成优化建议 |
| `completed` | "优化完成" | 优化完成，显示结果 |

### 文件解析技术实现

**API 路由**: `app/api/parse/route.ts`

| 文件格式 | 解析库 | 说明 |
|---------|--------|------|
| Word (.docx) | mammoth | 解析 Word 文档中的文本内容 |
| 图片 (PNG/JPG) | tesseract.js | 使用 OCR 技术识别图片中的文字 |
| PDF | - | 暂不支持 |

**OCR 语言配置**: 使用 `chi_sim+eng`（简体中文 + 英文）

### AI 优化技术实现

**API 路由**: `app/api/optimize/route.ts`

- 并行执行四个优化任务，提高响应速度
- 使用 OpenAI SDK 调用大模型 API
- 支持自定义 API 端点和模型配置
- Temperature 设置：
  - 关键词匹配: 0.3（更精确）
  - 内容重写: 0.5（平衡创意）
  - STAR 优化: 0.5（结构化输出）
  - ATS 评分: 0.3（精确评分）
  - 建议生成: 0.5（实用建议）

## 组件说明

### FileUpload 组件
- 位置: `app/page.tsx` (内联组件)
- 功能: 文件上传 UI 组件
- 特性:
  - 支持拖拽上传
  - 支持点击选择文件
  - 拖拽时高亮显示
  - 可配置禁用状态

### OptimizationTabs 组件
- 位置: `app/page.tsx` (内联组件)
- 功能: 优化结果标签切换
- 标签: 全部、关键词匹配、简历重写、STAR 原则、ATS 评分

### ATSScoreCard 组件
- 位置: `app/page.tsx` (内联组件)
- 功能: ATS 评分展示卡片
- 特性:
  - 显示总分和等级
  - 五个评分维度可视化
  - 颜色标识（绿色/黄色/橙色/红色）

### SuggestionsList 组件
- 位置: `app/page.tsx` (内联组件)
- 功能: 改进建议列表
- 特性:
  - 编号列表
  - 清晰易读的布局

## 项目启动

```bash
# 配置环境变量
cp .env.example .env.local
# 编辑 .env.local，填入你的 API 配置

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问
http://localhost:3000
```

## 使用流程

1. **准备输入**:
   - 粘贴或上传简历内容到左侧输入框
   - 粘贴或上传 JD 到右侧输入框（支持图片识别）

2. **开始优化**:
   - 点击"开始优化"按钮
   - 等待 AI 分析和生成结果（约 10-30 秒）

3. **查看结果**:
   - 查看关键词匹配分析
   - 查看优化后的简历内容
   - 查看 STAR 格式优化
   - 查看 ATS 评分
   - 查看改进建议

4. **迭代优化**:
   - 根据建议调整简历
   - 重新优化，对比评分变化

## 文件说明

- `app/page.tsx`: 主页面组件，包含文件上传、左右分栏布局和优化结果展示
- `app/api/parse/route.ts`: 文件解析 API 路由，处理 Word 和图片的文本提取
- `app/api/optimize/route.ts`: AI 优化 API 路由，处理四个维度的优化
- `app/globals.css`: 全局样式
- `tailwind.config.ts`: Tailwind CSS 配置
- `.env.example`: 环境变量配置示例
